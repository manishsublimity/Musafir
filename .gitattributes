# Normalise line endings so the repo stays consistent across Windows and CI.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff2 binary
*.cdr binary
